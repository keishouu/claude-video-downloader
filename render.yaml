services:
  - type: web
    name: video-export-renderer
    env: node
    plan: starter
    buildCommand: npm install && npx playwright install --with-deps chromium
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: EXPORT_TOKEN
        sync: false
