services:
  - type: web
    name: video-export-renderer
    runtime: docker
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: EXPORT_TOKEN
        sync: false
